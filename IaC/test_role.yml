# ansible-playbook -i hosts test_role.yml
   - hosts: all
     become: true
     roles:
     # pass the variable user_name to the role
      - { role: gemeinde_backend, user_name: 'pi' }
      - { role: translator_user, user_name: 'lang1', main_user: true , jitsi_channel: 'ukr'} # This is the main user and it will displayed on the Raspberry Pi Display
      - { role: translator_user, user_name: 'lang2', jitsi_channel: 'eng' } # All the other users are accessible via VNC and Web service at the port XXX

